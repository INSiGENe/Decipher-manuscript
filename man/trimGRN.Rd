% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intracellular_tailoring.R
\name{trimGRN}
\alias{trimGRN}
\title{Trim Gene Regulatory Network (GRN) Data Frame}
\usage{
trimGRN(grn_df, pValue, topEdges, minTargets)
}
\arguments{
\item{grn_df}{A data frame representing the gene regulatory network, expected to have
columns for p-values and source-target relationships.}

\item{pValue}{A numeric threshold for p-values; edges with p-values above this threshold will be discarded.}

\item{topEdges}{The maximum number of edges to retain, based on the highest absolute coefficients.
If NULL, no limit is applied.}

\item{minTargets}{The minimum number of targets required per source; sources with fewer targets than this
will be removed from the network.}
}
\value{
A trimmed GRN data frame.
}
\description{
This function trims a GRN data frame based on specified criteria.
It filters the GRN by p-value, limits the number of edges based on their absolute coefficients,
and removes sources with fewer targets than a specified minimum.
}
\note{
The function assumes that 'coef_abs' is a column in `grn_df` used for sorting edges
in `applyTopEdgesFilter`. If your data frame has a different column for edge coefficients,
you'll need to modify the function accordingly.
}
\examples{
# grn_df is a data frame representing a gene regulatory network
trimmedGRN <- trimGRN(grn_df, 0.05, 100, 3)

}
