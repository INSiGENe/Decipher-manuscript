% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_visualizations.R
\name{generate_heatmap_plot}
\alias{generate_heatmap_plot}
\title{Generate a heatmap plot of deltaPagoda values}
\usage{
generate_heatmap_plot(
  data,
  tfs,
  condition_names,
  absolute_max,
  condition_label,
  top_n
)
}
\arguments{
\item{data}{A data frame containing at least the columns
\`"TF"\`, \`"Comparison"\`, and \`"DeltaPagoda"\`.}

\item{tfs}{Character vector. The subset of regulon names to include in the plot.}

\item{condition_names}{Character vector. The factor levels and order for the
\`Comparison\` axis.}

\item{absolute_max}{Numeric. The maximum absolute value for the fill scale;
the gradient limits will be \`c(-absolute_max, absolute_max)\`.}

\item{condition_label}{Character. A label (e.g. "moderate" or "severe") used
in the plot title to indicate by which condition the top regulons were sorted.}

\item{top_n}{Integer. The number of top regulons (by absolute value) used, displayed in the title.}
}
\value{
A `ggplot` object representing the heatmap, or `NULL` if `tfs` is empty.
}
\description{
Creates a ggplot2 heatmap for a specified set of regulons (`tfs`) across
multiple conditions. The fill scale is centered at zero and bounded by
`±absolute_max`. Returns `NULL` if no regulons are provided.
}
