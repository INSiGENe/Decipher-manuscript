% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_data_wrangling.R
\name{extract_regulon_data}
\alias{extract_regulon_data}
\title{Extract Regulon Edge & Node Data for a Single Cell Type}
\usage{
extract_regulon_data(
  regulons,
  capped_regulons_all_clusters,
  significant_regulon_markers_by_cluster,
  selected_ct
)
}
\arguments{
\item{regulons}{Character vector of regulon gene names to extract.}

\item{capped_regulons_all_clusters}{Nested list of data.frames `[cluster][[gene]]` containing
filtered target (`source == gene`) rows with at least
some capping applied.}

\item{significant_regulon_markers_by_cluster}{Nested list of data.frames `[cluster][[gene]]` containing
marker stats, including columns `regulon`, `tg_gene`, and
`avg_log2FC`.}

\item{selected_ct}{String, the name of the cluster/cell type to pull from.}
}
\value{
A \code{list} with components:
\describe{
  \item{\code{edges}}{A data.frame of two columns, \code{from} (regulon) and \code{to} (target).}
  \item{\code{combined_data}}{A data.frame of \code{regulon}, \code{tg_gene}, and \code{avg_log2FC}.}
}
}
\description{
Given a set of regulon genes and two sources of cluster‐level data, this
function builds:
1. an `edges` data.frame of ligand→target pairs, and  
2. a `combined_data` data.frame of regulon statistics (`regulon`, `tg_gene`, `avg_log2FC`).
}
