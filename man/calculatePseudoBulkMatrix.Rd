% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pre_processing_meta_cells.R
\name{calculatePseudoBulkMatrix}
\alias{calculatePseudoBulkMatrix}
\title{Calculate Pseudo Bulk Matrix}
\usage{
calculatePseudoBulkMatrix(
  rnaCountsMatrix,
  distanceMatrix,
  numNearestNeighbors,
  numMetaCells
)
}
\arguments{
\item{rnaCountsMatrix}{A matrix containing RNA counts data, with rows
representing genes and columns representing cells.}

\item{distanceMatrix}{A precomputed distance matrix where each element
represents the distance between cells.}

\item{numNearestNeighbors}{Integer, the number of nearest neighbors to include
for each sampled cell.}

\item{numMetaCells}{Integer, the number of cells (columns from rnaCountsMatrix)
to sample for pseudo bulk calculation.}
}
\value{
A matrix where each column represents a pseudo bulk sample, containing
the sum of counts for each gene across a cell and its nearest neighbors.
}
\description{
This function creates a pseudo bulk matrix from a given RNA counts matrix
and a distance matrix. It samples a subset of columns (representing cells)
from the RNA counts matrix and calculates the sum of counts for each
cell and its nearest neighbors.
}
\examples{
# rnaCountsMatrix and distanceMatrix are pre-existing matrices
pseudoBulkMatrix <- calculatePseudoBulkMatrix(rnaCountsMatrix, distanceMatrix, 5, 10)
}
