% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_utilities.R
\name{calculate_pct_change}
\alias{calculate_pct_change}
\title{Calculate Percentage Change}
\usage{
calculate_pct_change(new_val, old_val)
}
\arguments{
\item{new_val}{Numeric vector of new or current values.}

\item{old_val}{Numeric vector of baseline values (same length as `new_val`).}
}
\value{
Numeric vector of percentage changes. Values can be finite,
  `Inf`, or `0`, matching the length of the inputs.
}
\description{
Vectorised helper that returns the percentage change from a baseline
(`old_val`) to an updated value (`new_val`).
}
\details{
The function handles division-by-zero explicitly:
* If `old_val` is `0` and `new_val` is positive, the result is `Inf`.
* If both `old_val` and `new_val` are `0`, the result is `0`.
}
\examples{
calculate_pct_change(c(120, 80, 0), c(100, 100, 0))
#> 20 -20 Inf

}
